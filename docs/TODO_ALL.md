# СПАС — TODO ALL: что ещё нужно сделать

> Этот документ — полный список того, что ещё **не сделано** в проекте СПАС, со ссылками на дорожную карту `improvement_roadmap.md` и конкурсные критерии «Моя страна — Моя Россия» 2026, номинация «Моё здоровье», 14–17 лет.
>
> **Текущее состояние (на 8 мая 2026):**
>
> - PR #1 (MVP) — слит / на ревью.
> - PR #2 (блоки C/D/E/G) — открыт, CI 3/3 зелёный.
> - 70 unit-тестов проходят. ruff + format clean. Content validator OK.
> - Бот, дашборд, лендинг, REST API, PWA, Marp-дек, финмодель, roadmap, governance — готовы.
>
> **Текущая экспертная оценка: ~66–70 / 80 балл жюри.** Цель — 72+. Чтобы её взять, нужно ещё всё, что ниже.

---

## Легенда

- 🚨 **БЛОКЕР** — без этого жюри сильно снизит оценку или попросят пилот не давать.
- 🟡 **ВАЖНО** — даёт +2…+4 балла, но не критично.
- 🟢 **NICE-TO-HAVE** — +0.5…+1 балл, можно отложить.
- 🤖 **МОГУ Я (Devin)** — реализуется кодом / документами, не требует людей.
- 👥 **ТРЕБУЕТ ЛЮДЕЙ** — переговоры, договоры, съёмка, медицинская валидация.
- 💰 **ТРЕБУЕТ БЮДЖЕТА** — нужны деньги или платные API.

---

## БЛОК A. Контент и медицинская валидация

### A1. 🚨 👥 Договор с врачом-ментором — НЕ СДЕЛАНО

- **Задача:** найти практикующего травматолога / реаниматолога из ФМБА или Минздрава. Прислать ему `content/scenarios.json` + `content/panic_protocol.json`. Получить подписанный документ-расписку «контент проверен и одобрен».
- **Шаги:**
  1. Составить список 5–10 врачей (через знакомых, через РМАПО, через школьного медработника, через волонтёрскую медицину).
  2. Написать письмо-обращение (~1 страница): кто я, что за проект, что прошу.
  3. Передать врачу JSON-сценарии в удобном формате (распечатать или PDF; могу сгенерировать `tools/scenarios_to_pdf.py`, скажи если надо).
  4. Получить письменное «ОК» (хоть на бумаге, хоть скан подписанной расписки в `docs/medical_review.pdf`).
- **Срок:** 1–3 дня моя работа + 5–10 дней ожидания ответа врача.
- **Балл:** +2 (соц. значимость) + 2 (устойчивость) +1 (реализуемость).
- **Снимает риск:** R1 (medical accuracy) с red на yellow.

### A2. 🚨 👥 Документировать правки врача — НЕ СДЕЛАНО

- Параллельно с A1: после читки врачом получить хотя бы 5–10 точечных правок, внести их в `content/scenarios.json` и закоммитить с автором правок в commit message.
- Обновить `risk_register.md` — снять R1 с red на yellow.

### A3. 🟡 🤖 💰 Yandex SpeechKit TTS озвучка шагов — НЕ СДЕЛАНО

- **Что:** для каждого шага сценария — кнопка «🔊 Слушать», которая шлёт `voice` с озвучкой через Yandex SpeechKit TTS (голос `oksana` или `ermil`, скорость 1.0).
- **Что у меня уже есть:** STT (распознавание) — модуль `bot/stt.py`. TTS (синтез) — нужно добавить новый модуль `bot/tts.py`.
- **Стоимость:** Yandex free-tier — 100 ₽ кредитов = ~50 000 символов = ~200 шагов. Должно хватить на демо.
- **Шаги:**
  1. Получить `YANDEX_TTS_API_KEY` (тот же ключ, что для STT).
  2. Написать `bot/tts.py` с `synthesize(text: str, voice: str = "oksana") -> bytes` (возвращает OGG Opus).
  3. Добавить кнопку «🔊 Слушать» к каждому шагу сценария.
  4. Кэшировать сгенерированные voice в SQLite по hash(text, voice) — чтобы не платить дважды.
  5. Тесты: подделать API-ответ через `aioresponses`.
- **Срок:** 1 день.
- **Балл:** +2 (инновация) +1 (доступность).

### A4. 🟡 👥 Видео-инструкции для top-10 сценариев — НЕ СДЕЛАНО

- **Что:** 30-секундные loop-видео на VK Видео для top-10 сценариев: СЛР взрослого, удушье Геймлих, эпилептический приступ, артериальное кровотечение, ожог, анафилаксия, утопление, обморок, отравление, гипогликемия.
- **Шаги (для пользователя):**
  1. Купить или одолжить манекен СЛР Resusci Anne / Prestan (~5–15 тыс. ₽) или попросить у школьной медкабинета.
  2. Снять на телефон в горизонтальной ориентации, статичная камера. Освещение — окно или кольцевая лампа.
  3. Озвучить закадровым голосом по тексту шагов из `content/scenarios.json`.
  4. Смонтировать в CapCut или iMovie + добавить субтитры.
  5. Залить на VK Видео (бесплатно), скопировать ID видео.
- **Что добавлю я после съёмки:** в `content/scenarios.json` поле `video_url` для каждого сценария → бот отправит ссылку.
- **Срок:** 3–5 дней (с участием 1–2 волонтёров).
- **Балл:** +3 (инновация) +1 (контент).

---

## БЛОК B. Доказательства реального использования

### B1. 🚨 👥 Запустить пилот в школе — НЕ СДЕЛАНО

- **Задача:** 30–50 пользователей за 2 недели. ≥5 пройденных сценариев / пользователь, NPS ≥ 7.
- **Шаги:**
  1. Договориться с классным руководителем / директором (1 школа достаточно). Шаблон письма — `docs/support_letter_template.md`.
  2. На уроке ОБЖ / классном часе показать бот, попросить пройти пилот. Дать QR-код на бота.
  3. Через 2 недели сделать выгрузку из бота: `/admin_export` (нужно добавить такую команду — могу сделать в 0.5 дня).
  4. Скриншоты дашборда → в jury_deck.
- **Срок:** 1 день настройка + 14 дней сбор.
- **Балл:** +3 (соц. значимость) +3 (эффективность).

### B2. 🟡 👥 15+ глубинных интервью с подростками — НЕ СДЕЛАНО

- Полу-структурированные интервью «как ты вёл бы себя при `<сценарий>` сейчас vs. через бот». 30 минут × 15 интервью = 7.5 часов.
- Согласие на запись (для несовершеннолетних — согласие родителя, шаблон в `docs/consent_form_template.md` — могу подготовить).
- Расшифровки в `docs/cusdev/transcripts/`.
- Сводная таблица «инсайт → доработка» в `docs/cusdev/insights.md`.
- **Балл:** +2 (методология) +1 (соц. значимость).

### B3. 🚨 👥 Pre/post тест эффективности — НЕ СДЕЛАНО

- На пилоте (B1) снять знание ДО (Google Forms, 10 вопросов из `survey_bank.md`) и ЧЕРЕЗ 14 дней использования бота.
- Цель: рост ≥ 25 п.п. на тех же вопросах.
- Вывести цифры в `dashboard.py` — раздел «Эффективность».
- **Срок:** 0.5 дня настройка + 2 недели.
- **Балл:** +3 (эффективность).

### B4. 🟡 👥 Letters of Intent от 2–3 партнёров — НЕ СДЕЛАНО

- Школа / директор + Юный Спасатель / региональное МЧС + детская поликлиника.
- Не «обещали поддержать», а на бланке за подписью.
- Шаблон уже есть: `docs/support_letter_template.md`.
- **Срок:** 1 неделя ожидания ответов.
- **Балл:** +2 (устойчивость).

### B5. 🟡 👥 Промо-видео 60–90 сек — НЕ СДЕЛАНО

- Экранкаст бота + закадровый голос «Артём, 16 лет, потерял сознание брат…».
- Снять на телефон + iMovie/CapCut.
- VK Video + YouTube; ссылка в `docs/jury_deck.md` слайд 6.
- Сценарий в Demo-tour (E7) — могу подготовить текстом, см. `docs/demo_tour_script.md` (после E7).
- **Срок:** 1 день.
- **Балл:** +2 (презентация).

---

## БЛОК C. Технические фичи

### C1. ✅ Голосовое управление — СДЕЛАНО (PR #2)

`bot/stt.py` + handler принимает voice → распознаёт → ищет сценарий.

### C2. 🟡 🤖 💰 GigaChat Vision — НЕ СДЕЛАНО

- **Что:** юзер фоткает рану/ожог → бот через GigaChat Vision API определяет тип/степень → советует сценарий.
- **Внимание:** НЕ диагноз, а триаж + «звони 112».
- **Стоимость:** GigaChat Pro / SaluteSpeech API — ~10 ₽/запрос. Нужен соответствующий тариф.
- **Шаги:**
  1. Получить GigaChat Pro key (или попросить у Сбера в рамках партнёрства, F1).
  2. Написать `bot/vision.py` с `analyze_photo(file: bytes) -> {"category": str, "severity": str, "scenario_id": str}`.
  3. Добавить handler: пользователь шлёт фото → `vision.analyze_photo` → бот предлагает сценарий + дисклеймер.
  4. Тесты — мок API.
- **Срок:** 2 дня + договор с Сбером.
- **Балл:** +4 (инновация).

### C3. ✅ SOS+geo — СДЕЛАНО (PR #2)

`bot/sos.py` + `/sos_contact /sos_share /sos_clear`.

### C4. ✅ Геймификация — СДЕЛАНО (PR #2)

`bot/gamification.py` + `/profile`, XP, 8 уровней, 10 ачивок, серия дней.

### C5. ✅ Teacher mode — СДЕЛАНО (PR #2)

`bot/teacher.py` + `/teacher /join CODE /teacher_dashboard`.

### C6. ✅ Family share — СДЕЛАНО (PR #2)

`bot/sharing.py` + `/share_progress /share_certificate`.

### C7. 🟢 🤖 МЧС RSS push — НЕ СДЕЛАНО

- **Что:** RSS-poll публичных каналов МЧС (например, https://www.mchs.gov.ru/news/rss/), при тревоге в регионе бот шлёт push-уведомление подписчикам.
- **Шаги:**
  1. Написать `bot/mchs_rss.py` с asyncio-poll'ом каждые 15 минут.
  2. Таблица `subscriptions(user_id, region)` через миграцию `m006`.
  3. Команды `/subscribe_alerts <REGION>`, `/unsubscribe_alerts`.
  4. Маппинг новости → сценарий (наводнение → utopanie, пожар → ожог).
- **Срок:** 1.5 дня.
- **Балл:** +2 (соц. значимость).
- **Риски:** МЧС RSS может быть недоступен снаружи; нужен fallback на обычный HTTP/JSON или Telegram-канал @mchs_official через `aiogram` user-mode.

### C8. 🟡 🤖 Многоязычность RU/EN/UZ/KZ (полный i18n) — ЧАСТИЧНО

- **Что у меня уже:** `bot/i18n.py` с минимальным словарём для новых команд (PR #2).
- **Что нужно:**
  1. Прокачать `bot/i18n.py` — добавить все строки UI, не только новых команд.
  2. Команда `/lang ru|en|uz|kz` сохраняет в `user_settings.lang_code`.
  3. Добавить переводы EN, UZ, KZ для всех ключевых сообщений (~100 строк × 4 языка = 300 строк).
  4. EN-лендинг уже есть (`landing/index.en.html`). UZ/KZ — нужно перевести.
- **Срок:** 2 дня.
- **Балл:** +2 (масштаб) +1 (доступность).

### C9. ✅ Доступность (a11y) — СДЕЛАНО (PR #2)

`bot/accessibility.py` + `/accessibility` plain-text режим.

### C10. ✅ Платформенный SDK — СДЕЛАНО (PR #2)

`bot/api.py` REST API + `landing/widget.js` встраиваемый виджет.

---

## БЛОК D. Аналитика и измеримость

### D1. 🟡 🤖 Public dashboard hardening + deploy — ЧАСТИЧНО

- ✅ Dashboard hardened (PR #2): tz-aware timestamps, cohort retention, NPS heatmap, behavioral funnel.
- ❌ Deploy на Streamlit Cloud / HuggingFace Spaces не сделан. Нужно:
  1. Завести аккаунт на Streamlit Community Cloud (бесплатно).
  2. Подключить репо → выбрать `dashboard.py`.
  3. В secrets указать `DB_URL=` (можно публичный read-only snapshot БД).
  4. Скопировать URL в `docs/jury_deck.md` слайд 8.
- **Срок:** 0.5 дня.
- **Балл:** +2 (эффективность) +1 (презентация).

### D2. ✅ A/B-тест pre/post — СДЕЛАНО (PR #2)

A/B варианты по эксперименту в `dashboard.py`. Storage хранит `ab_assignments`.

### D3. ✅ Cohort retention — СДЕЛАНО (PR #2)

Графики retention по неделям регистрации.

### D4. ✅ NPS heatmap — СДЕЛАНО (PR #2)

Heatmap «сценарий × NPS-балл».

### D5. ✅ Behavioral funnel — СДЕЛАНО (PR #2)

Воронка `/start → /sos → завершение → сертификат`.

### D6. 🟢 🤖 Real-time monitoring — НЕ СДЕЛАНО

- **Что:** Sentry интеграция уже есть в `bot/log_setup.py` (через `SENTRY_DSN`).
- **Что нужно:** настроить Sentry проект + добавить `SENTRY_DSN` в Fly.io secrets. Ссылка на dashboard для жюри.
- **Срок:** 0.2 дня.

---

## БЛОК E. Презентация и pitch

### E1. 🟡 🤖 Brand kit (логотип, палитра, social media kit) — НЕ СДЕЛАНО

- **Что:** SVG-логотип, color palette, fonts, social media kit (avatar, cover, post-template).
- **Шаги:**
  1. Сгенерировать SVG-логотип (могу сделать руками: красный плюс на тёмном фоне + monogram «СПАС»).
  2. Палитра: red `#ff3b30`, dark `#0b0d10`, accent green `#34c759`, muted `#b0b8c1` (уже в landing).
  3. Шрифты: PT Sans (свободный, кириллица).
  4. Social media kit: VK avatar 200×200, cover 1920×768, story 1080×1920.
  5. Один файл `docs/brand_guide.md` со всеми правилами + ассеты в `landing/icons/`.
- **Срок:** 1 день.
- **Балл:** +2 (презентация).

### E2. ✅ Marp slide deck — СДЕЛАНО (PR #2)

`docs/jury_deck.md` переписан под Marp. Сборка: `npx @marp-team/marp-cli@latest docs/jury_deck.md -o docs/jury_deck.pdf`.

### E3. ✅ Финансовая модель — СДЕЛАНО (PR #2)

`docs/finance_model.md` — P&L 3 года + чувствительность.

❌ Excel-версия (`docs/finance.xlsx`) — НЕ СДЕЛАНА. Можно экспортировать через скрипт `tools/finance_to_xlsx.py` (могу написать в 0.5 дня).

### E4. ✅ Roadmap Mermaid — СДЕЛАНО (PR #2)

`docs/roadmap.md` — Mermaid Gantt 2026–2028.

### E5. ✅ Governance + 152-ФЗ — СДЕЛАНО (PR #2)

`docs/governance_152fz.md`.

### E6. ✅ PWA — СДЕЛАНО (PR #2)

`landing/manifest.webmanifest` + `service-worker.js` + `widget.js` + install-prompt.

❌ Иконки `icons/icon-192.png` и `icons/icon-512.png` — НЕ НАРИСОВАНЫ. Lighthouse PWA-аудит будет жаловаться. Я могу:
- Сгенерировать через python+PIL красный плюс на тёмном фоне (могу сделать в 0.2 дня — `tools/generate_icons.py`).

### E7. 🟡 🤖 + 👥 Озвученный demo-tour — ЧАСТИЧНО

- ❌ **Сценарий записи (текст):** нужно подготовить — могу сделать в 0.3 дня. Файл `docs/demo_tour_script.md`.
- ❌ **Сама запись:** нужен человек с микрофоном + экранкаст. Я не могу записать.
- **Срок:** 1 день (моя часть — 2 часа, человек — рабочий день).

---

## БЛОК F. Устойчивость и партнёрства

### F1. 🟡 👥 Партнёрство со Сбером (GigaChat) — НЕ СДЕЛАНО

- **Что:** запросить education-tariff + co-marketing у `developers.sber.ru`.
- **Шаги:**
  1. Подготовить письмо-обращение (1 страница): кто я, проект, что прошу (бесплатные API, упоминание в их блоге).
  2. Отправить через https://developers.sber.ru/help/feedback или прямой email.
- **Балл:** +2 (устойчивость).

### F2. 🟡 👥 Российский Красный Крест — НЕ СДЕЛАНО

- moscow@redcross.ru или через сайт redcross.ru.
- Программа «Первая помощь в школах» — встроиться как digital-инструмент.
- **Балл:** +2 (устойчивость).

### F3. 🚨 👥 Росмолодёжь.Гранты — НЕ СДЕЛАНО

- **Что:** подача на грант 100–500 тыс. ₽ на пилот, после или параллельно с конкурсом.
- **Шаги:**
  1. Регистрация на https://грантыдетям.рф / https://фондкультурныхинициатив.рф / https://rosmolodez.ru
  2. Подготовить заявку (8 разделов: проблема, решение, команда, бюджет, KPI, partners, sustainability, presentation).
  3. Использовать `docs/finance_model.md` как базу бюджета.
  4. Срок зависит от текущего цикла грантов — обычно весна и осень.
- **Балл:** +2 (устойчивость) — одна из самых жирных тем для жюри.

### F4. 🟡 👥 Юный Спасатель / ВДЦ «Орлёнок» — НЕ СДЕЛАНО

- Встроить бот как поддерживающий инструмент в их летние программы.
- Контакты: orlyonok.ru, или через региональные МЧС-структуры.
- **Балл:** +2 (масштаб).

---

## БЛОК G. Code quality и DevOps

### G1. ✅ pytest-cov — ЧАСТИЧНО

- ✅ Добавлен в `requirements-dev.txt`.
- ❌ CI-job отдельный — не настроен. Нужно:
  1. Добавить в `.github/workflows/ci.yml` шаг `pytest --cov=bot --cov-fail-under=70 --cov-report=xml`.
  2. Опционально: загрузка coverage в codecov.io.
- **Срок:** 0.2 дня.

### G2. ✅ Pre-commit — СДЕЛАНО (PR #2)

`.pre-commit-config.yaml` с ruff + standard hooks.

### G3. ✅ Docker-compose dev — СДЕЛАНО (PR #2)

`docker-compose.yml`.

### G4. ✅ Throttle / anti-spam — СДЕЛАНО (PR #2)

`bot/middleware/throttle.py` sliding-window per-user.

❌ **Ещё не зарегистрирован в Dispatcher** — нужно добавить в `bot/__main__.py` строку `dp.message.middleware(ThrottleMiddleware())`. Сейчас middleware есть, но не подключен. **Это блокирующий баг.**

### G5. ✅ Sentry / structured logging — ЧАСТИЧНО

- ✅ JSON-logging через `bot/log_setup.py`, `LOG_FORMAT=json`.
- ✅ Sentry SDK хук есть.
- ❌ В Fly.io secrets `SENTRY_DSN` не выставлен. Нужно вручную после деплоя.

### G6. ✅ Health-check endpoint — СДЕЛАНО (PR #2)

`bot/health.py` на `:8080`.

### G7. ✅ Migrations — СДЕЛАНО (PR #2)

`bot/migrations.py` + таблица `_migrations`.

### G8. 🟢 🤖 README badges — НЕ СДЕЛАНО

- Добавить в `README.md` бейджи: CI, coverage, license, Python version.
- **Срок:** 0.1 дня.

### G9. 🟢 🤖 Renovate / Dependabot — НЕ СДЕЛАНО

- `.github/dependabot.yml` для авто-обновления pip-зависимостей.
- **Срок:** 0.1 дня.

---

## БЛОК H. Дополнительные задачи (нашёл при работе)

### H1. 🟡 🤖 Audit logging — НЕ СДЕЛАНО

- При каждом изменении roles (например, учитель добавляет/удаляет ученика, админ модерирует AED) — логировать в таблицу `audit_log(user_id, action, target, ts)`.
- Полезно для 152-ФЗ compliance (E5).
- **Срок:** 0.5 дня.

### H2. 🟡 🤖 Backup script — НЕ СДЕЛАНО

- Скрипт `tools/backup_db.py` — копирует SQLite БД с шифрованием в S3-совместимое хранилище (Selectel / Yandex Object Storage).
- Cron на Fly.io machines.
- **Срок:** 0.5 дня + аккаунт в S3-провайдере.

### H3. 🚨 🤖 /forget_me команда — НЕ СДЕЛАНО

- В `docs/governance_152fz.md` обещано удаление по запросу пользователя.
- Нужно: команда `/forget_me` → подтверждение → удаление user из всех таблиц + событий.
- **Срок:** 0.3 дня.
- **Блокирующее** — без неё в 152-ФЗ есть несоответствие.

### H4. 🟡 🤖 Admin export — НЕ СДЕЛАНО

- Команда `/admin_export` (только для `ADMIN_ID`) — выдаёт CSV-снимок таблиц для пилота / гранта.
- **Срок:** 0.3 дня.

### H5. 🟡 🤖 Webhook mode для Fly.io — НЕ СДЕЛАНО

- Сейчас бот работает в polling-режиме. Для production:
  1. Настроить webhook через `setWebhook(url=https://spas-ai.fly.dev/webhook)`.
  2. Изменить `bot/__main__.py` на `dp.start_webhook(...)`.
  3. Обновить `fly.toml` — добавить публичный port 8443.
- **Срок:** 0.5 дня.

### H6. 🟢 🤖 Тесты для handlers — НЕ СДЕЛАНО

- Сейчас тесты покрывают только модули (gamification, sos, accessibility и т.д.), но не сами aiogram-handlers.
- Можно добавить интеграционные тесты через `aiogram.test_utils`.
- **Срок:** 1 день.

### H7. 🟡 🤖 OpenAPI spec для REST API — НЕ СДЕЛАНО

- В `bot/api.py` добавить endpoint `/api/openapi.json` с OpenAPI 3.0 описанием.
- Пригодится школам для интеграции.
- **Срок:** 0.3 дня.

### H8. 🚨 👥 Согласие на обработку ПДн при `/start` — НЕ СДЕЛАНО

- В `docs/governance_152fz.md` упомянуто, но не реализовано в боте.
- При `/start` показать кнопку «Я согласен с обработкой ПДн (ссылка на документ)» — обязательно для 152-ФЗ.
- Хранить в `user_settings.consent_pdn_at`.
- **Срок:** 0.5 дня.

---

## Топ-15 за 7 дней (если время ограничено)

В порядке убывания ROI:

| # | Что | Кто | Срок | Балл |
|---|-----|-----|------|------|
| 1 | A1+A2 врач-ментор | 👥 ты | 1 неделя | +5 |
| 2 | B1 пилот в школе | 👥 ты | 14 дней | +6 |
| 3 | B5 промо-видео | 👥 ты | 1 день | +2 |
| 4 | H8 consent на /start | 🤖 я | 0.5 дня | +1 |
| 5 | H3 /forget_me | 🤖 я | 0.3 дня | +1 |
| 6 | G4 register Throttle in Dispatcher (баг) | 🤖 я | 0.1 дня | +0.5 |
| 7 | A3 TTS озвучка | 🤖 я | 1 день | +3 |
| 8 | C2 GigaChat Vision | 🤖 я | 2 дня | +4 |
| 9 | C8 полный i18n | 🤖 я | 2 дня | +3 |
| 10 | E1 brand kit + лого | 🤖 я | 1 день | +2 |
| 11 | E6 иконки PWA | 🤖 я | 0.2 дня | +0.5 |
| 12 | D1 deploy dashboard | 👥 ты | 0.5 дня | +3 |
| 13 | F1+F2 письма | 👥 ты | 1 день + 2 нед. | +4 |
| 14 | F3 Росмолодёжь.Гранты | 👥 ты | 3 дня + цикл | +2 |
| 15 | C7 МЧС RSS push | 🤖 я | 1.5 дня | +2 |

Если успеешь все 15 — выйдешь на ~78–80 / 80, то есть **гарантированно в финал и претензия на 1-е место**.

---

## Что я могу сделать в следующей сессии без блокеров

Если скажешь «продолжай» — начну с:

1. **G4-fix:** зарегистрировать Throttle в Dispatcher (1 коммит, 5 строк).
2. **H3:** `/forget_me` команда (1 коммит, ~30 строк + тест).
3. **H8:** Consent при `/start` (1 коммит, ~50 строк + тест).
4. **E6 icons:** сгенерировать PNG-иконки 192/512 через PIL.
5. **E1:** SVG-логотип + `docs/brand_guide.md`.
6. **C8 (полный):** прокачать i18n + EN-перевод всех строк UI + `/lang` команда.
7. **A3:** `bot/tts.py` + кнопка «🔊 Слушать» в шагах сценариев (с graceful degrade при отсутствии `YANDEX_TTS_API_KEY`).
8. **C7:** `bot/mchs_rss.py` + `/subscribe_alerts`.
9. **C2:** `bot/vision.py` + handler для photo (graceful degrade при отсутствии Pro key).
10. **G1 final:** добавить pytest --cov в CI workflow.
11. **G8/G9:** README badges + Dependabot.
12. **H1:** Audit log таблица.
13. **H4:** `/admin_export` CSV.
14. **H7:** OpenAPI spec для REST API.
15. **E7:** `docs/demo_tour_script.md` (сценарий для записи).
16. **D6:** настроить Sentry secrets в fly.toml (потребует SENTRY_DSN).

Это всё ещё ~3–4 дополнительные сессии. После этого код становится **защищённым** для жюри: «всё, что можно автоматизировать — автоматизировано, остаётся только живой пилот и врач».

---

## Что НЕ могу сделать никак

- Все, что в 👥 (требуют людей): A1, A2, A4, B1–B5, F1–F4.
- A3, C2, D6 без получения соответствующих API-ключей (работают, но не активны).
- C7, если МЧС RSS-канал недоступен извне.
- B5/E7 финальная видеозапись — нет камеры/микрофона.

---

_Сгенерировано Devin AI Engineer · session 2fd0e9877b094ec694e0dc8bd41ac461 · 2026-05-08_
